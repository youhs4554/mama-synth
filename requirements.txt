SimpleITK>=2.2
PyYAML>=6.0
scikit-learn>=1.2
scipy>=1.10
scikit-image>=0.20
# pyradiomics PyPI release is broken for Python >= 3.10 (issue #903).
# Install from the main branch of the GitHub repo instead:
#   pip install git+https://github.com/AIM-Harvard/pyradiomics.git@master
# Note that for pyradiomics to install, you might need to install a c++ compiler beforhand, e.g. see here https://visualstudio.microsoft.com/visual-cpp-build-tools/.
# The line below uses PEP 508 direct-reference syntax so that
# `pip install -r requirements.txt` works out-of-the-box (requires git).
pyradiomics @ git+https://github.com/AIM-Harvard/pyradiomics.git@master
#lpips>=0.1  # not used — torchmetrics handles LPIPS
frd-score>=1.0
torchmetrics>=1.0
torch<2.10
pandas
nnunetv2>=2.4
numpy
xgboost<2.0  # pin: pkl files were saved with xgboost 1.x; 2.x+ breaks __setstate__
tqdm